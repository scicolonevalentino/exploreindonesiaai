## Change

Replace the headline text "Book your Indonesia trip" with "Have you planned your trip to Indonesia?" in two places:

- `src/routes/index.tsx` (line 159) — homepage
- `src/routes/prototype.tsx` (line 503) — prototype page

No other changes (styling, structure, SEO meta) will be touched.

## Question

Should I update both occurrences, or only the homepage (`index.tsx`)? The prototype route is a non-production scratch page.